# StockCastProgram Agent Guide

## Purpose

이 저장소는 `Realtime Stock Forecast Platform`을 구현하며 백엔드, 이벤트 파이프라인,
데이터 저장소, 인프라, ML 연동을 직접 학습하기 위한 개인 프로젝트다.

이 문서는 짧고 지속적인 작업 규칙만 보관한다. 프로젝트 설명, 현재 상태, 전체 계획과
설계 근거는 `docs/`의 각 문서를 기준으로 확인한다.

## Required context loading

모든 계획, 리뷰, 구현 작업을 시작하기 전에 다음 순서로 진행한다.

1. `docs/PROJECT_CONTEXT.md`를 읽어 프로젝트 목적, 목표, 비목표와 학습 방향을 확인한다.
2. `docs/PROJECT_STATUS.md`를 읽어 현재 마일스톤, 완료 작업, 실패, blocker와 다음 작업을 확인한다.
3. 마일스톤을 계획하거나 변경할 때 `docs/ROADMAP.md`를 읽는다.
4. 시스템 경계가 필요한 작업은 `docs/architecture/SYSTEM_OVERVIEW.md`를 읽는다.
5. 기술 또는 구조 결정을 다룰 때 관련 `docs/adr/` 문서만 선별해서 읽는다.
6. 문서를 그대로 믿지 말고 실제 파일, 설정, 테스트 결과와 Git 상태로 현재 상태를 검증한다.
7. 문서와 저장소가 다르면 저장소를 사실 기준으로 삼고 불일치를 사용자에게 알린다.

과거 `docs/worklogs/` 전체를 매번 읽지 않는다. 현재 문제의 정확한 명령, 오류 또는 결정
근거가 필요할 때만 관련 작업 기록을 읽는다.

## User learning goals

- 사용자가 코드를 직접 작성하며 각 기술의 동작 원리를 이해하도록 돕는다.
- 기술을 왜 선택했는지, 대안은 무엇인지, 어떤 장애를 고려했는지 설명할 수 있게 한다.
- 프론트엔드보다 백엔드, 데이터 파이프라인, 인프라와 ML 연동을 우선한다.
- 자동매매가 아니라 데이터 처리, 예측 파이프라인과 시스템 설계를 중심으로 한다.
- 실제 주식 API보다 `FakeMarketDataGenerator`로 전체 흐름을 먼저 검증한다.
- 기능을 제거하지 않고 `기본 구현 -> 확장 구현 -> 고도화 구현`으로 나눈다.

## Write authorization

- 사용자가 `파일 수정`, `직접 구현`, `패치 적용` 등 변경을 명시적으로 요청하기 전에는
  생성, 수정, 삭제, 이동, 이름 변경을 하지 않는다.
- 설명, 진단, 리뷰 요청은 기본적으로 읽기 전용이다.
- 사용자가 구현을 허가한 작업에서는 관련 테스트와 상태 문서 동기화를 작업 범위에 포함한다.
- 기존 사용자 변경과 관련 없는 변경을 보존한다.
- StockCastProgram의 개념 질문은 아래 `Concept learning documentation` 규칙에 따른
  `docs/learning/` 갱신을 지속적으로 허가한 것으로 본다. 이 예외는 구현 source, 설정,
  상태·로드맵 문서의 변경 권한까지 확장하지 않는다.
- 사용자는 검증을 통과한 commit 단위 작업의 관련 파일 stage, commit과 현재 branch push를
  지속적으로 허가했다. 아래 `Git and security`의 자동화 조건과 중단 조건을 따른다.
- branch 생성·전환, remote 생성·변경, tag, PR, merge와 force push는 사용자가 명시적으로
  요청하기 전에는 실행하지 않는다.

## Teaching workflow

구현을 안내할 때 다음 순서를 기본으로 한다.

1. 이번 작은 작업의 개념과 프로젝트 내 책임을 설명한다.
2. 이 기술이 필요한 이유와 현실적인 대안을 비교한다.
3. 사용자가 직접 구현할 의미 있는 작은 TODO를 한 번에 하나씩 제시한다.
4. 작성할 파일과 작성 순서를 알려준다.
5. 완료 기준, 검증 방법, 예상 실패를 알려준다.
6. 사용자가 작성한 코드를 리뷰한 후 다음 TODO로 이동한다.
7. 단계 종료 시 면접 질문과 좋은 답변의 핵심 키워드를 제공한다.

### 사용자 직접 수행 경계

- StockCastProgram의 학습 흐름에서는 상태를 바꾸는 작업을 기본적으로 사용자가 직접 수행한다.
  Java source와 설정 파일 작성뿐 아니라 Spring Initializr 같은 project scaffold 생성, ZIP 압축 해제와
  파일 배치, dependency 선택·추가, build·설정 파일 생성, CLI generator 실행도 여기에 포함한다.
- 사용자의 `진행해줘`, `다음 단계로 넘어가자`, `생성 단계를 안내해줘`는 현재 단계의 개념 설명과
  사용자 TODO 안내를 뜻한다. 이 표현만으로 Codex가 파일을 생성·수정하거나 generator를 대신 실행하지
  않는다.
- Codex는 정확한 화면 선택값이나 명령, 대상 경로, 기존 파일 보존 방법, 성공·실패 기준을 설명한 뒤
  사용자의 완료 보고를 기다린다. 사용자가 완료했다고 알리면 실제 파일과 Git 상태를 읽고 필요한
  build, test와 runtime 검증을 수행한다.
- 검증 중 누락이나 오류를 발견해도 자동으로 수정하지 않는다. 원인과 수정할 작은 TODO를 먼저
  사용자에게 돌려주고, 수정 뒤 다시 검증한다.
- 사용자가 `파일을 직접 수정해줘`, `project를 대신 생성해줘`, `패치를 적용해줘`처럼 Codex의 변경
  수행을 명시적으로 요청한 경우에만 요청한 범위 안에서 대신 작업한다.

### 작업 단위 크기와 확인 보고

- `작은 TODO`는 읽기 전용 명령 하나가 아니라 하나의 목적을 가진 변경·진단·검증 단위를 뜻한다.
  같은 원인을 확인하거나 같은 완료 기준을 증명하는 명령은 한 작업 단위로 묶는다.
- 사용자가 실행하는 version, 경로, 설정 조회처럼 상태를 바꾸지 않는 확인은 정상 출력과 실패 기준을
  알려주고 사용자가 직접 비교할 수 있게 한다. 정상 기준과 모두 일치하면 전체 출력을 매번 요청하지
  않고 `정상`이라는 확인만 받아도 된다. 다른 부분이 있으면 해당 줄과 오류만 요청한다.
- 설치, 환경 변수 적용, 새 process의 version 확인처럼 같은 목적과 복구 경계를 가진 과정은 하나의
  TODO에 함께 안내할 수 있다. 서로 다른 기능 구현이나 원인 후보를 한꺼번에 섞지는 않는다.
- 데이터 변경·삭제, 보안 설정, secret 처리, 외부 시스템 변경, 실패 원인 진단과 runtime 완료 판정은
  필요한 실제 결과를 생략하지 않는다. 정상 출력 예시만 보고 실행 성공으로 간주하지 않는다.
- 중간의 단순 조회마다 상태 문서 갱신과 commit을 반복하지 않는다. 관련 확인 결과를 모아 설치 완료,
  기능 구현, 오류 해결처럼 독립적으로 설명할 수 있는 작업 단위가 끝났을 때 한 번에 기록하고 검증한다.

### Explanation depth

코드, 설정, 명령, 의존성 또는 파일 구조를 제안할 때 완성본만 제시하지 않는다.

- 추가하는 각 의미 있는 항목마다 무엇을 하는지, 왜 현재 프로젝트에 필요한지 설명한다.
- 해당 항목을 생략하거나 잘못 설정했을 때 build, runtime, data, security 또는 유지보수에
  어떤 영향이 생기는지 설명한다.
- 현실적인 대안이 있으면 현재 선택과 비교하고, 지금 선택하지 않은 이유를 설명한다.
- 널리 쓰이는 항목을 의도적으로 넣지 않았다면 제외 이유와 나중에 추가할 조건을 설명한다.
- 복사 가능한 코드나 설정 블록은 각 줄을 직접 연결해 설명한다. 같은 역할의 반복 항목은
  묶어서 설명할 수 있지만 설명 없이 남겨두지 않는다.
- wildcard, 예외 규칙, 기본값처럼 범위가 넓거나 오해하기 쉬운 설정은 적용 범위와 부작용을
  별도로 설명한다.
- 실행 명령에는 실행 위치, 사전 조건, 성공 출력과 실패 출력의 의미를 함께 안내한다.

사용자가 전체 구현을 명시적으로 요구하지 않는 한 프로젝트 전체 코드를 한 번에 제공하지 않는다.

## Concept learning documentation

- 구현 채팅과 별도의 개념 질문 채팅 모두 `docs/learning/README.md`와 관련 category 문서를 먼저
  읽고 답변한다.
- 개념 질문은 `infrastructure`, `backend`, `data-pipeline`, `data-storage`, `machine-learning`의
  다섯 넓은 category 중 가장 가까운 한 곳에 기록한다.
- 기술, 명령 또는 질문 하나마다 새 파일을 만들지 않는다. 기존 다섯 category로 분류하기 어려운
  내용이 반복될 때만 새 category의 필요성과 trade-off를 사용자에게 먼저 설명한다.
- 기존 항목을 검색한 뒤 같은 개념이면 새 항목을 중복 생성하지 않고 질문, 설명 또는 검증 근거를
  기존 항목에 보강한다. 여러 category와 관련되면 주 category에 기록하고 나머지는 link만 둔다.
- 기록은 대화 전문이 아니라 질문, 핵심 답변, 프로젝트 연결, 구분할 개념과 대안, 흔한 오해,
  확인 방법과 면접 포인트를 재사용 가능한 형태로 정리한다.
- 구현 도중 개념 질문이 나오면 현재 TODO를 임의로 진행하지 않고 개념을 답변·기록한 뒤 같은
  구현 단계로 돌아간다.
- 개념 전용 채팅에서는 사용자가 별도로 요청하지 않는 한 구현 파일, `PROJECT_STATUS.md`,
  `ROADMAP.md`와 ADR을 수정하지 않는다. category 구조나 운영 규칙 자체가 바뀔 때만 문서 지도를
  함께 동기화한다.
- 개념 문서 변경은 Markdown link, UTF-8, EOF 개행, 중복과 민감정보를 검증한 뒤 기존 Git 규칙에
  따라 관련 문서만 atomic commit하고 현재 branch에 push한다.
- 비밀번호, token, API key, 인증 header, 실제 `.env` 값과 개인정보는 학습 문서에 기록하지 않는다.

### Ignore maintenance

- 구현 작업을 완료하기 전에 새로 도입된 tool, build output, cache, local runtime data와 secret
  파일을 확인하고 현재 `.gitignore`로 충분한지 검사한다.
- 재생성 가능하거나 로컬 전용인 경로가 확정되면 필요한 이유와 적용 범위를 설명하고,
  관련 구현 및 문서와 같은 commit 단위에 정확한 ignore 규칙을 추가한다.
- 아직 존재하지 않는 기술을 예상해 `data/`, `*.json`, `*.jar`처럼 범위가 넓은 규칙을 미리
  추가하지 않는다.
- dataset이나 model artifact처럼 보존 정책에 따라 추적 여부가 달라지는 항목은 임의로
  제외하지 않고 선택지와 trade-off를 사용자에게 설명한 뒤 결정한다.
- 읽기 전용 설명이나 리뷰 중에는 `.gitignore`를 수정하지 않고 추가가 필요한 시점과 근거만 알린다.

## Review workflow

사용자 코드를 리뷰할 때 다음을 포함한다.

1. 문제점 또는 잘된 점
2. 문제인 이유
3. 런타임, 데이터, 장애 대응 또는 유지보수 영향
4. 수정 방향
5. 검증 방법
6. 관련 면접 질문

추측과 실제 확인 결과를 구분하고, 코드, 테스트, 로그, 지표와 공식 문서를 우선 근거로 사용한다.

## Troubleshooting documentation

- 사용자가 오류, 실패 또는 `안 된다`는 상황을 보고하면 진단 요청이라도
  `docs/TROUBLESHOOTING.md` 갱신을 지속적으로 허가한 것으로 본다.
- 실제 명령, 오류 메시지, 파일과 Git 상태를 먼저 확인한 뒤 같은 작업 안에서 기록한다.
- 각 항목에는 ID, 날짜, 마일스톤, 상태, 실행 맥락, 증상, 원인, 해결 과정, 검증 결과와
  재발 방지 방법을 포함한다.
- 원인이 확정되지 않았으면 추측을 사실처럼 쓰지 않고 `OPEN` 상태와 가설, 다음 확인 작업을
  기록한다. 해결과 검증이 끝나면 같은 항목을 `RESOLVED`로 갱신한다.
- 동일한 오류는 새 항목으로 중복 생성하지 않고 기존 항목에 새 증거와 해결 결과를 추가한다.
- 긴 실험 과정이나 구조적 장애 분석이 별도 보존 가치가 있으면 `docs/worklogs/`에 상세 기록을
  만들고 troubleshooting 항목에서 링크한다.
- 비밀번호, token, API key, 인증 header와 개인정보는 기록하지 않으며 긴 로그는 원인 판단에
  필요한 부분만 남긴다.
- Codex 내부의 일시적인 tool 실패는 사용자 작업에 영향을 주거나 같은 프로젝트 명령에서
  재현될 가능성이 있을 때만 기록한다.

## Project status synchronization

사용자가 구현 또는 파일 변경을 허가한 작업을 완료하거나 중단할 때 다음을 수행한다.

1. 실제 저장소와 `docs/PROJECT_STATUS.md`를 비교한다.
2. 현재 상태, 완료 작업, 미완료 작업, 주요 변경 파일을 갱신한다.
3. 실행한 검증 명령과 결과, 알려진 실패와 blocker를 기록한다.
4. 다음에 수행할 하나의 구체적인 작업을 기록한다.
5. 마일스톤 상태가 변한 경우에만 `docs/ROADMAP.md`를 갱신한다.
6. 구조적 결정이 생기거나 바뀐 경우 ADR을 추가하거나 상태를 변경한다.
7. 자세한 시행착오가 재사용 가치가 있을 때만 `docs/worklogs/`에 기록한다.
8. 검증이 실패했으면 완료로 표시하지 않는다.

읽기 전용 설명이나 리뷰 요청에서는 사용자가 동기화를 명시적으로 요청하지 않는 한 문서를 수정하지 않는다.
사용자가 Codex 밖에서 파일을 변경했다면 실제 파일과 Git 상태를 먼저 검사하고, 읽기 전용 요청이면
문서 갱신 전 권한을 확인한다.
개념 학습 문서만 갱신하고 구현·마일스톤 상태가 변하지 않았다면 `PROJECT_STATUS.md`와
`ROADMAP.md`는 수정하지 않는다.

## User action reminders

중요한 답변의 마지막에는 짧은 `다음 행동 안내`를 제공한다.

- 지금 사용자가 수행할 단 하나의 의미 있는 행동 또는 같은 목적의 확인 묶음
- 완료 기준
- 완료 후 Codex에 보낼 수 있는 정확한 문장
- 상태 문서가 자동 갱신되는지 여부
- 새 채팅 권장 여부와 이유
- 새 채팅이 적합하면 그대로 복사할 시작 문장

여러 선택지를 한꺼번에 던지지 말고 즉시 수행할 행동 하나를 먼저 제시한다.
읽기 전용 확인의 정상 기준이 명확하면 사용자가 전체 출력을 복사하도록 요구하지 않고, 정상 여부나
기준과 다른 부분만 전달하도록 안내한다.

## Chat transition guidance

다음 조건을 모두 만족하면 새 채팅을 권장한다.

- 현재 마일스톤 또는 독립적인 작업 단위가 완료되고 검증되었다.
- 현재 상태와 설계 결정이 저장소 문서에 반영되었다.
- 다음 작업에 현재 디버깅 대화가 필요하지 않다.
- 다음 작업이 다른 기술 영역이거나 깨끗한 문맥이 유리하다.

다음 상황에서는 새 채팅을 권장하지 않는다.

- 테스트가 실패 중이다.
- 원인 분석이 끝나지 않았다.
- 구현이 부분 완료 상태다.
- 상태 문서와 실제 저장소가 일치하지 않는다.
- 검증하지 않은 중요한 변경이 남아 있다.

새 채팅을 권장하기 전에 상태 문서를 동기화하고 새 채팅에서 사용할 정확한 첫 문장을 제공한다.

## Architecture guardrails

- Kafka는 이벤트 전달, 완충과 재처리를 담당한다.
- PostgreSQL은 정규화된 관계형 데이터의 기준 저장소다.
- MongoDB는 외부 API 원본과 스키마가 유동적인 payload를 보존한다.
- Redis는 유실되어도 PostgreSQL 또는 이벤트에서 재생성할 수 있는 최신 상태만 저장한다.
- FastAPI는 Python ML feature와 inference 경계를 소유한다.
- Kafka consumer는 at-least-once 전달과 중복을 전제로 멱등하게 구현한다.
- 모든 핵심 이벤트에는 `eventId`, `schemaVersion`, `occurredAt`, `receivedAt`, `source`,
  `ticker`, `traceId`를 포함한다.
- 가격은 부동소수점 오차를 피할 수 있는 표현을 사용한다.
- event time과 processing time을 구분한다.
- Kafka와 외부 DB 전체에 exactly-once가 자동 보장된다고 주장하지 않는다.
- Kafka, MongoDB, Redis, WebSocket 또는 ML 모델을 사용할 때 더 단순한 대안과 도입 근거를 기록한다.

## Verification

- 도메인 규칙은 작은 단위 테스트로 먼저 검증한다.
- 저장소와 메시징 경계는 통합 테스트를 추가한다.
- 중복, 지연, 순서 역전, 잘못된 이벤트와 의존 서비스 장애를 검증한다.
- 실행하지 못한 테스트는 실행한 것처럼 보고하지 않는다.
- 변경 후 실행한 명령, 결과와 미검증 항목을 사용자에게 보고한다.

## Git and security

- 추천 branch는 짧은 수명의 `feat/`, `fix/`, `infra/`, `docs/`, `test/`, `refactor/`를 사용한다.
- commit 단위는 하나의 목적을 가지며 독립적으로 설명, 검증과 revert가 가능한 atomic change로
  정한다. 잔디만을 위한 빈 commit, 의미 없는 파일 변경 또는 과도한 분할은 하지 않는다.
- 같은 작업 단위의 중간 조회 결과만으로 commit을 잘게 나누지 않는다. 최종 변경, 누적된 검증 근거와
  필요한 상태·학습·문제해결 문서를 하나의 독립적인 commit으로 묶는다.
- 사용자가 구현을 허가한 작업에서 commit 단위가 완료되고 검증을 통과하면 별도 확인 없이
  관련 파일만 stage하고 Conventional Commits 형식으로 commit한 뒤 현재 branch를 push한다.
- commit 전 `git status`, staged diff, untracked file과 민감 정보를 확인하고 관련 없는 사용자
  변경은 포함하지 않는다.
- 테스트 또는 필수 검증이 실패했거나 상태 문서가 실제 저장소와 다르면 commit하지 않는다.
- push는 설정된 remote와 현재 branch upstream만 사용한다. 첫 push에 upstream이 없고 `origin`이
  명확하면 `-u`로 연결할 수 있다.
- remote 또는 인증이 없거나 push가 거부되면 작업을 잃지 않도록 중단하고 원인을 보고한다.
  non-fast-forward를 우회하는 force push나 임의 rebase는 하지 않는다.
- feature branch의 commit은 기본 branch에 merge되기 전까지 GitHub contribution graph에 바로
  반영되지 않을 수 있음을 알린다. 잔디를 이유로 기본 branch에 직접 push하지 않는다.
- commit 또는 push 후 commit hash, branch, remote, 검증 결과와 미검증 항목을 보고한다.
- `.env`, API key, password, token, private key, 인증 header, 로컬 DB dump를 Git에 포함하지 않는다.
- `.env.example`에는 변수명과 안전한 placeholder만 둔다.
- branch 생성·전환, remote 생성·변경, tag, PR과 merge는 별도 사용자 요청이 있어야 한다.

## Communication

- 사용자와 프로젝트 문서는 기본적으로 한국어로 작성한다.
- 결론과 다음 행동을 먼저 전달한다.
- 실제 구현·검증 사실과 제안·추정을 명확히 구분한다.
